{
    "name": "legal-platform",
    "private": true,
    "packageManager": "pnpm@9.1.0",
    "scripts": {
      "dev": "turbo dev",
      "build": "turbo build",
      "lint": "turbo lint",
      "test": "turbo test",
      "dev:web": "pnpm --filter web dev",
      "lint:web": "pnpm --filter web lint",
      "dev:api": "pnpm --filter api dev",
      "vercel-build": "pnpm --filter api run build"
    },
    "devDependencies": {
      "turbo": "^2.0.0"
    }
  }
  